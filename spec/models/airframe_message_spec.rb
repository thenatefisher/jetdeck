require 'spec_helper'

describe AirframeMessage do

  it "can be sent to contact" do
  end

  it "requires an airframe" do
  end

  it "requires a recipient" do
  end

  it "requires a creator" do
  end

  it "requires a subject" do
  end

  it "cannot be sent without user activation" do
  end

  it "generates a photos url on creation" do
  end

  it "generates a spec url on creation" do
  end

  it "shows 'Sent' status and date when message is sent" do
  end

  it "shows 'Opened' status and date when message is opened" do
  end

  it "shows 'Downloaded' status and date when message is downloaded" do
  end

  it "shows 'Bounced' status and date when message is bounced" do
  end

  it "shows 'NA' status when and date created" do
  end  

  it "can send email" do
  end

  it "hides photos link in email if not enabled" do
  end
  
  it "hides spec link in email if not enabled" do
  end

  it "does not require a spec to be associated" do
  end

end
