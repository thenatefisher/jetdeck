<td></td>
<td>
    <a href="<%= link %>">
        <% if (parseInt(version)==0) { %>
        Original
        <% } else { %>
        Revision <%= version %>
        <% } %>
    </a>
</td>
<td><small><%= updated %></small></td>
<td><button class="btn btn-mini pull-right send">Send</button></td>
