require 'test_helper'

class UserTest < ActiveSupport::TestCase

  test "can sell records" do
    assert true
  end

  test "cannot sell same information twice" do
    assert true
  end

  test "cannot buy same information twice" do
    assert true
  end

  test "can add a new spec" do
    assert true
  end

  test "can access shared specs" do
    assert true
  end

  test "can share a spec with multiple users" do
    assert true
  end

  test "can create sessions" do
    assert true
  end

  test "cannot see other user unshared aircraft" do
    assert true
  end

  test "can validate purchased data" do
    assert true
  end

  test "gets credits for a data sale" do
    assert true
  end

  test "is charged for a data purchase" do
    assert true
  end

  test "can add a contact to address book" do
    assert true
  end

  test "cannot purchase data with insufficient credits" do
    assert true
  end

end
